# 開発者向けメモ

利用者向けの説明は [README.md](README.md) にあります。ここには、ビルド手順と、
実装をそう決めた理由を残します。

## 構成

C++20 と素の Win32 です。メディア セッションは C++/WinRT で直接叩き、画面は
メモリ DC への GDI 描画 + `AlphaBlend`、画像はデコードだけ WIC に任せて、
拡大縮小と色の下ごしらえは自前で計算します。外部ライブラリは使いません。

| ファイル | 役割 |
| --- | --- |
| [src/main.cpp](src/main.cpp) | 起動・二重起動の抑止・COM の初期化 |
| [src/app.cpp](src/app.cpp) | 各部品の接続、タイマー、スレッド間の受け渡し |
| [src/media.cpp](src/media.cpp) | WinRT のメディア セッション監視・操作 |
| [src/bar.cpp](src/bar.cpp) | タスクバーに重ねるバー |
| [src/popup.cpp](src/popup.cpp) | ホバーで出るアルバム アートの小窓 |
| [src/tray.cpp](src/tray.cpp) | トレイ アイコン (音量の数字) とツールチップ |
| [src/menu.cpp](src/menu.cpp) | 右クリック メニュー (バーとトレイで共用) |
| [src/image.cpp](src/image.cpp) | 画素の計算 (Lanczos・ガウス・アンシャープ・角丸) |
| [src/artwork.cpp](src/artwork.cpp) | アルバム アート・音符・数字アイコンの組み立て |
| [src/theme.cpp](src/theme.cpp) | テーマ・アクセント カラー・DPI |
| [src/win32util.cpp](src/win32util.cpp) | タスクバーの位置取得・最前面維持・ホイール フック・音量の読み取り・出力先の切り替え |
| [src/config.cpp](src/config.cpp) | 設定の保存 (小さな JSON の読み書きを含む) |
| [src/errlog.cpp](src/errlog.cpp) | 想定外の失敗だけを書き残すログ |
| [src/icontool.cpp](src/icontool.cpp) | `muutask.ico` を書き出す道具 (ふだんのビルドには入らない) |

`MUUTASK_DEBUG=1` を設定して起動すると、タスクバーへの追従で「隠した理由」や
「組み立て直した」を `MuuTask.log` と `OutputDebugString` に出します。

## ビルド

```powershell
powershell -ExecutionPolicy Bypass -File build.ps1
```

必要なのは **Visual Studio 2022 Build Tools の C++ ワークロード**だけです
(MSVC・Windows SDK・Ninja・CMake が入ります)。無ければ:

```powershell
winget install --id Microsoft.VisualStudio.2022.BuildTools -e `
  --override "--quiet --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

| 出力 | 中身 |
| --- | --- |
| `dist\MuuTask\` | 配布フォルダー。`MuuTask.exe` + [README.txt](README.txt) |
| `dist\MuuTask-<版>.zip` | 配布用 (約 210 KB) |

**exe 1 つで動きます。** CRT を静的リンクしているので VC 再頒布可能パッケージも
要りません。依存するのは Windows に元からある DLL だけです
(`user32` / `gdi32` / `msimg32` / `shell32` / `ole32` / `windowscodecs` と、
C++/WinRT が使う `api-ms-win-core-winrt-*`)。

版番号は [src/version.h](src/version.h) の `MUUTASK_VERSION_NUM` が唯一の
出どころで、zip 名と [README.txt](README.txt) の `@VERSION@`、exe のバージョン
情報に埋め込まれます。**`v1` から始めて、配布物を作るたびに 1 ずつ上げます。**

`config.json` は**配りません** (v22 まで既定値のものを同梱していました)。設定を
変えたときに exe 自身が隣へ書くもので、既定値のまま配っても伝わることが無く、
「入っている以上は消してはいけないもの」に見えてしまうためです。zip に入れる
ものは [build.ps1](build.ps1) で名指ししてあり、開発機の `config.json` や
`MuuTask.log` は混ざりません。

### ビルドで踏んだ落とし穴

- **ソースは UTF-8 なので `/utf-8` が要ります。** 付けないと日本語コメントが
  システムのコード ページ (932) として読まれます。
- **`rc.exe` にも `/c65001` が要ります。** こちらは警告では済まず、UTF-8 の
  日本語コメントが 2 バイト文字として対に組まれた結果、**行末の改行まで巻き
  込んで次の `#define` 行ごと消えました** (実測。`version.h` の
  `MUUTASK_VERSION_NUM` が「未定義」になる)。`CMakeLists.txt` で指定しています。

### アイコン

`muutask.ico` はアプリ内の音符 ([src/artwork.cpp](src/artwork.cpp) の
`NoteIcon`) から作るので、トレイのアイコンと見た目が揃います。リソースとして
リンク前に要るため、ふだんのビルドには含めていません。音符の形や色を変えた
ときだけ:

```powershell
cmake --build build --target icontool
build\icontool.exe muutask.ico
```

256 px で一度だけ描いてから各寸法へ縮めます (小さい寸法をそれぞれ描き直すと
線の太さの見え方が揃わないため)。48 px 以上は PNG で入れています。全部を BMP に
すると ico だけで 290 KB になり、exe が倍に膨らみます (実測)。

## Python から C++ へ (v22)

v21 まで Python 3 + Tk + Pillow + pystray + WinRT の射影で書かれていました。
実測 (同じ機械で 2 回ずつ):

| | 起動の CPU | 常駐メモリ | 配布物 |
| --- | --- | --- | --- |
| Python (onefile, v19 まで) | 1.17 - 1.28 秒 | 23 MB | 13.3 MB |
| Python (onedir, v20-v21) | 0.47 - 0.48 秒 | 17 - 18 MB | 29 MB |
| **C++ (v22 以降)** | **0.08 - 0.14 秒** | **1.8 - 2.1 MB** | **0.42 MB** |

体感でいちばん大きかったのは、**バーが画面に出るまでの時間**です。

| | バーが出るまで |
| --- | --- |
| Python (v21) | 5.52 秒 |
| C++ (v22) | 0.17 秒 |

5 秒の正体は Python でも C++ でもなく、**画面 DC からの `GetPixel`** でした
(→ [タスクバーの地色を拾う](#タスクバーの地色を拾う))。

移植で見た目を変えたところは 1 つだけです。ボタンの下の座は、Tk が項目ごとの
アルファ合成をできないため 50% の網掛け (`stipple="gray50"`) で半透明に見せて
いました。GDI では `AlphaBlend` で本物の半透明にしてあります。原寸での見え方は
同じで、拡大すると網目が無くなります。

## 仕組み

- 再生情報は Windows の `GlobalSystemMediaTransportControlsSessionManager`
  (WinRT) から取得しています。曲・アーティスト・アルバム アート・再生位置・
  操作可否まですべて OS 側から受け取るため、プレーヤーごとの対応は不要です。
- WinRT の呼び出しは専用スレッド (MTA) で行い、変更通知はイベント + 1 秒ごとの
  保険ポーリングで拾っています ([src/media.cpp](src/media.cpp))。この監視
  スレッドが止まると、**表示も操作も凍りついたまま二度と戻りません**
  (→ [監視が止まらないようにする](#監視が止まらないようにする))。
- 各非同期呼び出しには 5 秒の上限を置き、`IAsyncOperation::wait_for` で
  **待つのをやめるだけ**にしています。取り消しを頼むと、応答しない相手では
  その取り消し自体で固まることがあるためです (Python 版で `asyncio.wait_for`
  が使えなかったのと同じ理由)。
- **アルバム アートは曲名より遅れて届きます。** 曲が変わった瞬間に読むと、
  前の曲の絵や途中の別画像が返ってきます (実測では 0.5 秒ほど遅れて本来の
  画像に差し替わりました)。そのため曲が変わってから 8 秒間は 0.3 秒ごとに
  読み直し、中身が変わっていれば拾い直します。
- **同じ絵が、大きい方から小さい方へ順に届きます。** Chrome の YouTube Music
  (PWA) で実測した並びは、曲を切り替えてから

  | 届く順 | 中身 | 大きさ |
  | --- | --- | --- |
  | 1 | ブラウザーのアプリ アイコン (Chrome ロゴ) | 256x256 |
  | 2 | アルバム アート | 150x150 |
  | 3 | 同じアルバム アート (JPEG で畳み直したもの) | 120x120 |

  で、2 と 3 の間はわずか 0.2〜0.4 秒です。3 は色ノイズの乗った粗い絵なので、
  受け取ってしまうと小窓が汚くなります。**いま持っているものと同じ絵で、かつ
  小さいものは見送ります**。1 は曲名がまだ空のうちに来るので、名無しの間は
  アートを受け取らないことで避けています (前の曲のアートを出したままにする)。
  **SMTC には大きさを要求する口が無く**、載せる絵はアプリ任せなので、150x150
  より大きいものはもらえません。ログアウト状態の Firefox に至っては、
  YouTube Music でもアートも曲名も渡してきません (実測)。
- **Firefox はシークした直後に「長さも位置も 0」のタイムラインを送ってきます。**
  Google からログアウトした状態の YouTube / YouTube Music で実測しました。
  正しい位置が 1 度届いた 0.25 秒後に、再生中のまま空のタイムラインで
  上書きされます (`LastUpdatedTime` も今の時刻に更新されるので、古い報せとして
  見分けることもできません)。素直に受け取ると曲の途中なのにバーが頭に戻り、
  一時停止して再生し直すまで戻りません。長さの分かっている曲で空が来たときは
  届かなかったものと見なし、直前の値を進め続けます。
- **ホイールは低レベル マウス フック (`WH_MOUSE_LL`) で拾っています**
  ([src/win32util.cpp](src/win32util.cpp))。バーは `WS_EX_NOACTIVATE` で
  フォーカスを持たないため、`WM_MOUSEWHEEL` は届きません。Windows はホイールを
  フォーカスのあるウィンドウへ送り、「非アクティブ ウィンドウをホバー時に
  スクロールする」が入っているときだけカーソルの下へ送るからです。この設定に
  左右されないよう、フックで拾って握り潰します。フックの処理は入力の流れを
  止めるので、中では位置の判定と音量キーの送出しか行いません (時間がかかると
  Windows にフックを外されます)。音量そのものは `VK_VOLUME_UP` /
  `VK_VOLUME_DOWN` を送るだけにしています。既定の再生デバイスの選び直しを OS に
  任せられ、Windows 標準の音量表示も出るためです。
- **押し込み (中ボタン) はミュートの入り切りに使っています** (v27 から)。
  受け取る範囲は回す方と**まったく同じ** (`Bar::OnMiddleClick` は
  `WheelTarget` をそのまま使う) で、通知領域もアプリのボタン列も含みます。

  中クリックはタスクバーの上で既に意味を持っていますが、**わざと潰しています**:

  | 場所 | 既定の Windows の動き | MuuTask |
  | --- | --- | --- |
  | 地の部分 (`Shell_TrayWnd` が直に返るところ) | 何もしない | 頂く |
  | アプリのボタン列 (`MSTaskSwWClass`) | そのアプリをもう 1 つ開く | **潰して頂く** |
  | 通知領域 (`TrayNotifyWnd`) | 各アプリに届く | **潰して頂く** |

  本人の判断です (TrayVolume の代わりとして、タスクバーのどこでも同じように
  押せる方を採った)。邪魔なときはメニューの「ホイールで音量・押し込みで
  ミュート」で回す方ごと切れます。

  MuuTask 自身のトレイ アイコンの押し込みは、フックとは**別の道**でも
  受けています — シェルからの `WM_MBUTTONUP` ([src/tray.cpp](src/tray.cpp))。
  ふだんはフックが先に握り潰すのでこちらへは来ませんが、設定を切っていても
  自分のアイコンだけは効くようにするためのものです (左クリックが常に
  再生 / 一時停止なのと同じ扱い)。二重に発火することはありません。フックが
  取ったときはシェルに届かず、シェルに届いたときはフックが取っていません。

  横取りするときは**押した方と離した方の両方**を握り潰します
  ([src/win32util.cpp](src/win32util.cpp) の `WheelHook::Thunk`)。押した方だけ
  消すと、下のウィンドウに「押していないのに離れた」が届いてしまいます。

  タスクバーの部分を見分ける `TaskbarHit` は、当たったウィンドウから root まで
  親をたどって入れ物の名前を見ます。時計や個々のトレイ アイコンのように、
  もっと深いものが返ることがあるためです。実測 (3072px 幅を 20 点で横断):
  地 11・アプリのボタン 6・通知領域 3・タスクバー外 0 と、狙いどおりに
  分かれました。いまのところ押し込みは全部まとめて頂いていますが、部分ごとに
  分けたくなったときはこれで切り分けられます。

- **横取りする範囲はタスクバー全体です** (v24 から。それ以前はバーの上だけ)。
  矩形で判定していません。`WindowFromPoint` → `GetAncestor(GA_ROOT)` の
  クラス名が `Shell_TrayWnd` (副モニターは `Shell_SecondaryTrayWnd`) かどうかで
  見ます ([src/win32util.cpp](src/win32util.cpp) の `PointOnTaskbar`)。
  実測で、スタート付近・アプリのボタン列 (`MSTaskSwWClass`)・空きスペース・
  通知領域 (`TrayNotifyWnd`) のどこを指しても root は `Shell_TrayWnd` で返り、
  タスクバーの 1px 上と画面の中ほどは外れました (10/10 と 0/2)。
  この見方だと**重なりの判定が一緒に片付きます** — スタート メニュー・
  ウィジェット・タスクのサムネイル・隠れているインジケーターの一覧はどれも
  タスクバーの子ではない別ウィンドウなので、開いていれば自動的にそちらの
  取り分になります。自動的に隠す設定で引っ込んでいるときも同じです。
  そのうえで `ForegroundIsFullscreen()` を明示的に見て、全画面のアプリが
  前面のときは何も横取りしません (視界の外の操作で音量が動かないように)。
  バーはタスクバーの子ではなく上に重ねた別ウィンドウなので、従来どおり
  矩形 + `IsCovered` で別に判定します ([src/bar.cpp](src/bar.cpp) の
  `WheelTarget`)。
- 画面はダーク / ライトと **Windows のアクセント カラー**、高 DPI に追従します
  ([src/theme.cpp](src/theme.cpp))。
- タスクバーへの表示は、**タスクバーと同じ高さ・同じ地色の最前面ウィンドウ**を
  空きスペースにぴったり重ねて実現しています ([src/bar.cpp](src/bar.cpp))。
  0.25 秒ごとにタスクバーの位置・高さ・通知領域の左端を測り直して追従します。
  タスクバーをクリックするとタスクバー自身が持ち上がってバーが潜るので、
  `WindowFromPoint` で覆われたかを判定し、覆われていれば前に出し直します。

### タスクバーの地色を拾う

地色はその場所のピクセルを実際に読んで合わせています。**一色ではなく行ごと**に
読んで同じ並びを描き写しているので、タスクバー上端の細いハイライト
(実測で `#424242`、本体は `#212121`) がバーの上でも途切れません。一色で塗ると、
そこだけ塗り潰されて浮いて見えていました。

読み方には落とし穴があります。**画面 DC への `GetPixel` は 1 画素あたり
16.7 ms かかります** (実測)。5 点 × 高さ 60 行 = 300 画素で **5.0 秒**です。
これがバーの初回表示と、幅・位置を変えたときの作り直しをまるごと待たせて
いました (v21 の実測で、起動からバーが出るまで 5.52 秒)。

`BitBlt` で矩形を一度だけメモリへ写し、あとはメモリ上を走査すると **20 ms** に
なります。返る色は 1 画素ずつ読んだものと**完全に一致**します — 上端の
ハイライト行 (`#424242`) も含めて突き合わせて確かめました。`CAPTUREBLT` は
要りません (付けても同じ値でした)。

### なぜタスクバーに「重ねる」のか

Windows 11 は公式のタスクバー拡張 (ツール バー / デスク バンド) を廃止しており、
`SetParent` でタスクバーの子ウィンドウにする従来の方法も、現在のビルドでは
エラー 87 で拒否されます (他プロセスのシェル ウィンドウへの再親子付けが
塞がれている)。そのため本アプリは **タスクバーに重ねる最前面ウィンドウ**という
方式を取っています。見た目はタスクバーの中に入っていますが、内部的には
タスクバーの一部ではありません。

その副作用が、**スタート メニューを開くとバーが後ろに隠れ、閉じただけでは
戻らない**という制限です。スタート メニューを開くと、タスクバー
(`Shell_TrayWnd`) は z バンドが **1 → 6 (`ZBID_IMMERSIVE_EDGY`)** に上がります。
そして**スタート メニューを閉じてもバンドは 6 のまま**で、別のウィンドウが
活性化されるまで下りてきません (実測。フォアグラウンドが元のアプリに戻っていても
下りません)。上のバンドへは `SetWindowPos` では入れず、`SetWindowBand` は
UIAccess 権限 (署名 + 信頼された場所へのインストール) が無いと
`ERROR_ACCESS_DENIED` で弾かれます。オーナー設定 (`GWLP_HWNDPARENT`) も
他プロセスのウィンドウには効きません。**ユーザー権限のアプリからは回避できない**
ため、この間は要求を出さずに待ちます。バンドが 1 に戻れば 0.25 秒以内に
復帰します。

管理者権限で動いているアプリが前面にあるときにホイールでの音量操作が効かない
ことがあるのも同じ筋で、ユーザー権限のフックはより高い権限のウィンドウへの
入力を受け取れないためです (UIPI)。

## 監視が止まらないようにする

メディア監視のスレッドが死ぬと、**バーは最後の曲を映したまま固まり、ボタンも
効かなくなり、何をしても戻りません**。実際に v9 でこれが起きました。凍った
プロセスのスタックを取ると監視スレッドだけが消えていて、監視の周回が特定の
例外しか受け止めていなかったのが原因でした。相手のアプリは曲を切り替えるたびに
セッションを作り直すので、閉じかけのセッションを触ると想定外の失敗が返って
きます。それが 1 回抜けただけでスレッドごと終わり、ボタンの要求も宛先を失う、
という筋道です。

そのため監視は次の三重で守っています。

- **周回は何が飛んできても続ける。** 失敗は握り潰さずログに残し、3 回続けて
  転んだらセッション マネージャーごと取り直します。
- **待ちっぱなしを作らない。** WinRT の各呼び出しに 5 秒の上限を設け、
  返ってこない相手は見捨てて先へ進みます。取り消しは頼みません。
- **それでも落ちたらワーカーごと立て直す。** 1 秒待って作り直します。
  諦めるのは終了するときだけです。

閉じかけのセッションは `GetPlaybackInfo()` / `GetTimelineProperties()` が
**null を返してきます** (実測。v13 のログには 1 日で 5 回残っていました)。
失敗にして周回ごと転ばせると、3 回続いたときにセッションを張り直すことになり、
アルバム アートも覚えている再生位置も捨ててしまいます。捨てた直後に Firefox の
空タイムラインが来ると、直したはずのバーがまた頭に戻ります。そのため読めない
周回は静かに飛ばし (表示が 1 秒ぶん古くなるだけ)、5 回続いたときだけ張り直します。

追いかける対象はアプリ ID ではなく**セッションそのもの**で見比べます。同じ
アプリでも曲を変えるとセッションは作り直されるため、ID で見ていると閉じた方を
掴んだままになり、イベントも届かず、ボタンの宛先も失われます。

想定外の失敗が出たときだけ、config.json と同じフォルダーに `MuuTask.log` を
書きます ([src/errlog.cpp](src/errlog.cpp))。ふだんは作られません。

## 描画の細部

バーの中身は毎回メモリ DC の 32bpp DIB へ描き、`WM_PAINT` ではそれを転送する
だけにしています (ちらつき防止)。描く順は奥から
**地色 → 文字 → アート枠の覆い → アルバム アート → シーク バー → 座 → 記号**
です。文字を描いたあとにアート枠の手前まで地色を塗り直すので、流れてきた文字が
アートの上に出ることはありません。

### 文字送り

曲名とアーティスト行は、**ボタンの手前までに収まっていれば止まったまま**です。
はみ出すときだけ横に流れます (止めたままだと末尾がボタンの裏に隠れたままに
なるため)。流れる範囲はアルバム アートの右からバーの右端までで、ボタンの裏を
通ります。

**一周して先頭に戻ったところで 1 秒止まります。** タイトルとアーティスト行は
長さが違うので、止まる時機は別々に数えています (揃えると片方が中途半端な
位置で止まるため)。同じ曲の間は位置を保ち、曲が変わったときだけ頭に戻します。
送りは 34 px/秒 (96 DPI 基準) で、40 ms タイマーが進めます。

### アルバム アートの下ごしらえ

もらえるアートは大きくても 150x150 なので、大きな小窓では引き伸ばすことに
なります。そのままだと JPEG 由来の色ノイズが粒として浮くので、**色だけを
ならしてから引き伸ばし、輝度だけを締め直します** ([src/image.cpp](src/image.cpp))。
色に触らずに輪郭を締めると、なめらかにしたはずの色の縁が立ち直ってしまうため、
順番と対象を分けています。

引き伸ばす前に、**縁の帯も落とします**。YouTube Music は 4:3 のジャケットを
正方形のキャンバスに置いて渡してくることがあり (実測では (18,18,18) の帯)、
そのままだと小窓の中でジャケットが小さくなります。角の色と同じで平らな列を
外側から落とし、絵そのものを食べないよう片側 30% で打ち切ります。

バーの中の小さなアートは**正方形の枠しか無い**ので、こちらは帯を落としたあとに
中央を切り出しています (トレイ アイコンはアルバム アートを使わないので対象外)。

### 音声の出力先を切り替える

右クリック メニューの「音声の出力先」は、Windows の**既定のサウンド デバイス**を
入れ替えています ([src/win32util.cpp](src/win32util.cpp) の `AudioOutputs` と
`SetDefaultAudioOutput`)。

- **一覧**は `IMMDeviceEnumerator::EnumAudioEndpoints(eRender, DEVICE_STATE_ACTIVE)`
  で、名前は `PKEY_Device_FriendlyName` から読みます。**メニューを開いた
  その瞬間にだけ**呼びます — この手の問い合わせは重く、既定を 1 つ引く
  `GetDefaultAudioEndpoint` + `GetId` だけで**実測 1.9 ms** かかります
  (音量の読み取り 59 µs の 32 倍)。
- **切り替えには公開 API がありません。** Windows の設定画面が内部で使っている
  `CPolicyConfigClient` (CLSID `870af99c-…`) の `IPolicyConfig::SetDefaultEndpoint`
  を叩きます。**文書化されていない**ので、vtable の並びを合わせるために前に
  並ぶメソッドも同じ形で宣言してあります (中身は使わないため引数の型は void*)。
  Windows 11 で動くことは実測で確かめました — 出力先が 2 つある環境で行き来
  させ、`GetDefaultAudioEndpoint` が付いてくることを見ています。消えても
  `CoCreateInstance` が失敗して false を返すだけで、他は動き続けます。
- 変えるのは **eConsole と eMultimedia の 2 つだけ**です。Windows の
  「既定のサウンド デバイスにする」と同じ範囲で、**eCommunications は触りません** —
  通話用に別のデバイスを選んである環境を壊さないためです (実測でも、切り替えの
  前後で communications の既定は動きませんでした)。
- `functiondiscoverykeys_devpkey.h` は **`mmdeviceapi.h` より後に置くこと。**
  `DEFINE_PROPERTYKEY` が定義済みである前提で書かれていて、単独で読むと
  「型指定子がありません」で止まります (実測)。

### 出力先が変わったら音量を掴み直す

`IAudioEndpointVolume` は**特定のデバイスを指したまま**なので、既定が入れ替わっても
古いデバイスの音量を返し続けます。これに追いつく方法を 2 つ測って選びました。

| 方法 | 費用 | 採否 |
| --- | --- | --- |
| 毎回 `GetDefaultAudioEndpoint` で id を引いて比べる | 1.9 ms/回 → 0.25 秒ごとで **0.77% CPU** | 不採用。常駐 CPU (1.7%) がほぼ倍になる |
| `IMMNotificationClient` で知らせを受ける | ふだん 0。**+1 スレッド・+11 ハンドル**、作業セットは測定差なし | 採用 |

知らせは MMDevice 側のスレッドから来るので、コールバックの中では
`std::atomic<bool>` の旗を立てるだけにしてあります。旗は `Read()` が見て、
掴み直しの合図にします。メニューから切り替えたときは知らせを待たず、
`App::SelectAudioOutput` が明示的に `Forget()` してから読み直します
(選んだ手応えを早くするため)。

実測: 出力先を 2 台の間で行き来させると、`VolumeMeter::Read()` の返す値が
0%/ミュート (Realtek) ↔ 18%/ミュート無し (LG) と付いてきました。

### 常駐量を削る回数を 2 回に増やした

音声まわり (MMDevice と、その裏の RPC) は**遅れて温まります**。起動 5 秒の
1 回目の返却ではまだ触られておらず、実測で 12 秒には作業セットが 8.7 MB まで
膨らんでいました。外から返させると 3.9 MB に戻るので、実体ではなく**返し忘れ**です
(private の実測は 3.7 MB で、出力先の機能を入れる前の 3.4 MB とほぼ同じ)。

そこで 2 回目を 30 秒に置きました ([src/app.cpp](src/app.cpp) の `kTrimSecondDelay`)。
実測で、返却直後の作業セットは **1.75 MB** に戻ります。

### トレイのアイコンは音量の数字

通知領域のアイコンは**システムの音量 (0-100)** を数字で描いたものです
([src/tray.cpp](src/tray.cpp) と [src/artwork.cpp](src/artwork.cpp) の
`NumberIcon`)。ホイールで音量を変えたとき、その結果がその場で読めます。

- **下地は敷きません。** 16x16 で 3 桁を読めるようにするには枠をまるごと字に
  使う必要があり、角丸の下地を敷くとその内側にしか置けなくなります。代わりに
  タスクバーの明暗に背けた色で字だけを置きます (時計と同じ見え方)。明暗は
  `AppsUseLightTheme` ではなく **`SystemUsesLightTheme`** を見ます — 片方だけ
  明るくできる別々の設定で、通知領域はこちらに従うためです
  ([src/theme.cpp](src/theme.cpp) の `IsSystemDarkMode`)。
- **`GetSystemMetrics(SM_CXSMICON)` の大きさで直に描きます。** 音符と同じ 64 で
  描いてシェルに縮めてもらうと、数字の輪郭が甘くなります (実測。`42` と `100`
  ではっきり差が出ました)。形の滑らかな音符と違い、細い画で出来ている字は
  縮小に弱いためです。音符 (音量が読めないときの代役) は従来どおり 64 のまま。
- **ミュート中は数字の下に ✕ を添えます。**色を落とす案は採っていません —
  灰色は「暗いテーマで少し沈んだ数字」との区別が付きにくいためです。✕ は
  フォントの字ではなく 2 本の帯として描きます (5px 角では字形が潰れます)。
  数字に斜線を重ねる案も、16x16 では数字自体を読めなくするので不採用。
  ✕ を入れるぶん数字は上に寄り、少し低くなります。最初は高さを 0.54 まで
  詰めましたが、実測で数字が痩せて読みにくくなったので 0.62 に戻し、代わりに
  ✕ を下端へ寄せて (中心 0.845・腕 0.15) 間を空けました。正確な値は
  ツールチップにも出します。
- 音量の値は `IAudioEndpointVolume` を COM で読みます
  ([src/win32util.cpp](src/win32util.cpp) の `VolumeMeter`)。動かす方は
  メディア キーのままです。既定デバイスが差し替わると掴んだインターフェイスが
  古くなるので、読めなかったら一度手放して掴み直します。
- 読み直しは**タスクバーへの追従と同じ 0.25 秒周期**です
  ([src/app.cpp](src/app.cpp) の `SyncVolume`)。音量はキーボード・ミキサー・
  他のアプリのどこからでも変わるので、通知を受ける口
  (`IAudioEndpointVolumeCallback` + `IMMNotificationClient`) を増やすより
  素直でした。値が実際に変わったときだけアイコンを描き直します。
  実測で `Read()` は **40 µs/回**、0.25 秒ごとなら **0.016% CPU** です
  (掴み直しだけは 2.1 ms かかりますが、読めなかったときにしか起きません)。
  常駐量への影響も無く、追加後も常駐 2.3 MB・ハンドルは増え続けません
  (250 → 248 で頭打ち)。

**もらえる絵は正方形とは限りません** — YouTube の動画では 150x83 (16:9) が
そのまま来ます (実測)。正方形に切り出すと左右 45% を捨てることになるので、
小窓では縦横の比を保ったまま `popup_height` に収めます。16:9 なら 400 設定で
400x225 になり、小窓もその形になります。Windows 11 のプレビューは XAML の中で
描かれていて**ウィンドウとして掴めない**ため (クラス名で辿れるのは 1x1 の
`ThumbnailDeviceHelperWnd` だけで、ホバー時に増える窓も無い)、大きさの既定値は
実測した値ではなく、超えない範囲で置いています。

拡大縮小は Lanczos (support 3) を自前で回しています。Pillow の `LANCZOS` と
同じ流儀で、縮小するときはフィルターの方を伸ばして折り返しを防ぎます。移植の
確認として、同じ画像を Pillow 版と突き合わせました (画素値 0-255 での差):

| | 大きさ | RGB 平均差 | RGB 最大差 |
| --- | --- | --- | --- |
| 300 px へ拡大 (色ならし + 輪郭締め) | 一致 | 2.235 | 36 |
| 44 px へ縮小 (バーの中) | 一致 | 0.111 | 1 |

縮小はほぼ完全一致で、差が出るのは拡大の経路だけです。ぼかしに Pillow は
箱ぼかし 3 回の近似を使い、こちらは素直にガウスを畳んでいるためで、差は細い線の
縁にだけ乗ります (10 倍に強調してようやく見える程度)。

## 常駐メモリ

起動から 5 秒後と、以後 10 分ごとに `SetProcessWorkingSetSize(-1, -1)` で
常駐ページを OS に返しています ([src/win32util.cpp](src/win32util.cpp))。
WinRT の初期化・フォントやコーデックの読み込みなど**起動時にしか触らないもの**が
そのまま常駐し続けるためで、実測では **20.8 MB → 1.8 MB** になりました。
捨てるのは物理メモリ上の常駐分だけなので、必要になれば読み直されます。

## 設定ファイルとレジストリ

**レジストリには一切書きません。** 設定は `MuuTask.exe` と同じフォルダーの
`config.json` に置くので、やめるときはフォルダーごと削除すれば痕跡が
残りません。書き出すファイルはこれと、想定外の失敗が出たときだけ作られる
`MuuTask.log` の 2 つだけで、どちらも同じフォルダーに置きます。
書き込めない場所 (Program Files 直下など) に置かれた場合は保存を黙って
諦め、その回の変更は効いたまま次回起動時に既定値へ戻ります。

読み取りだけはレジストリを使います — ダーク / ライトとアクセント カラーを
`HKCU\...\Themes\Personalize` と `...\DWM` から読む部分です
([src/theme.cpp](src/theme.cpp))。書き換えはしません。

v1 が `%APPDATA%\MuuTask\config.json` に残した設定があれば、起動時に新しい
場所へ移してから旧フォルダーを消します (空でなければ削除が失敗するので、
自分以外が置いたものは残ります)。

設定ファイルは「値が null / 真偽 / 整数 / 文字列だけの平たいオブジェクト」に
限られるので、その形だけを読む小さな実装を [src/config.cpp](src/config.cpp) に
置いています。汎用の JSON ライブラリを抱き込む理由がないためです。手で編集した
ときの BOM 付きファイルも読めます。

自動起動をアプリ側の設定として持たないのも、レジストリに何も書かないためです
(利用者にはスタートアップ フォルダーへのショートカットを案内しています)。
